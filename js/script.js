// array nomor
const nomor = [1, 4, 5, 8, 10, 30, 50, 100];

// cek nilai tertinggi pada array nomor
const nilaiTertinggi = Math.max(...nomor);

// cetak variabel nilai Tertinggi;
console.log(nilaiTertinggi);
